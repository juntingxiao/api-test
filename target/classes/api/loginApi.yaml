name: loginApi
url: /cpms-auth/login
method: post
#reqType: json
#headers: {
#  "content-type": "application/json"
#    "token": "${token}"
#}
headers:
  "Authorization": "Basic ${base64}"

#formParams:
#  "username": "18700000011"
#  "password": "hls_1014"

#queryParams:
#  "username": "${username}"
#  "password": "${password}"

#body: {
#  "username": "${username}",
#  "password": "${password}",
#  "mobile": "${mobile}"
#}

#以上就是接口的Object描述，把接口看做对象
#这里面的${username} 或者${token} 就是引用 或者读取 或者自动生成的数据填充进来